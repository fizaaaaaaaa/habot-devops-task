# Local Terraform cache — regenerated by `terraform init`, never commit
.terraform/

# State files — may contain sensitive data; use a remote backend in production
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*

# Variable files that might contain secrets
*.tfvars
*.tfvars.json

# Crash logs
crash.log
crash.*.log